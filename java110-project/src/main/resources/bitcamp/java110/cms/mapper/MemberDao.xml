<?xml version="1.0" encoding="UTF-8"?>
<!-- MemberDao 클래스가 사용할 SQL이 들어 있는 파일 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitcamp.java110.cms.dao.MemberDao">

    <resultMap type="manager" id="managerMap">
        <id column="mno"        property="no"/>
        <result column="name"   property="name"/>
        <result column="email"  property="email"/>
        <result column="tel"    property="tel"/>
        <result column="photo"   property="photo"/>
    </resultMap>

  <insert id="insert" useGeneratedKeys="true" keyColumn="mno" keyProperty="no" parameterType="member">
    insert into p1_memb(name,email,pwd,tel,cdt)
    values(#{name},#{email},password(#{password}),#{tel},now())
  </insert>
</mapper>









